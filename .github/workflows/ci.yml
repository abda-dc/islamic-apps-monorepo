name: CI (baseline)

on:
  push:
    branches: [ "main" ]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-baseline-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: ${{ matrix.name }} (Node ${{ matrix.node }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        node: [18, 20]
        include:
          # Services (MIT)
          - name: Services/hadith-api
            path: Services/hadith-api
          - name: Services/quran-api
            path: Services/quran-api

          # Apps (AGPL-3.0)
          - name: apps/al-azan
            path: apps/al-azan
          - name: apps/my-quran
            path: apps/my-quran
          - name: apps/quran-hifz
            path: apps/quran-hifz
          - name: apps/quran_app
            path: apps/quran_app
          - name: apps/quran_salah_dua
            path: apps/quran_salah_dua

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}

      - name: Detect package manager
        id: pm
        shell: bash
        working-directory: ${{ matrix.path }}
        run: |
          set -euo pipefail

          # If this project is not Node-managed (no package.json), skip Node steps safely.
          if [ ! -f "package.json" ]; then
            echo "has_node=false" >> "$GITHUB_OUTPUT"
            echo "No package.json found in $PWD — skipping Node install/build/test."
            exit 0
          fi

          echo "has_node=true" >> "$GITHUB_OUTPUT"

          if [ -f "pnpm-lock.yaml" ]; then
            echo "manager=pnpm" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if [ -f "yarn.lock" ]; then
            echo "manager=yarn" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "manager=npm" >> "$GITHUB_OUTPUT"

      - name: Install dependencies
        if: steps.pm.outputs.has_node == 'true'
        shell: bash
        working-directory: ${{ matrix.path }}
        run: |
          set -euo pipefail

          MANAGER="${{ steps.pm.outputs.manager }}"
          echo "Using package manager: $MANAGER"

          if [ "$MANAGER" = "pnpm" ]; then
            corepack enable
            pnpm --version
            pnpm install --frozen-lockfile
            exit 0
          fi

          if [ "$MANAGER" = "yarn" ]; then
            corepack enable
            yarn --version

            # Yarn Berry uses yarnrc.yml and supports --immutable
            if [ -f ".yarnrc.yml" ]; then
              yarn install --immutable
            else
              # Yarn classic
              yarn install --frozen-lockfile
            fi
            exit 0
          fi

          # npm
          if [ -f "package-lock.json" ] || [ -f "npm-shrinkwrap.json" ]; then
            npm ci
          else
            npm install
          fi

      - name: Build (if present)
        if: steps.pm.outputs.has_node == 'true'
        shell: bash
        working-directory: ${{ matrix.path }}
        run: |
          set -euo pipefail

          if node -e "const p=require('./package.json'); process.exit(p.scripts && p.scripts.build ? 0 : 1)"; then
            MANAGER="${{ steps.pm.outputs.manager }}"
            echo "Running build with: $MANAGER"
            if [ "$MANAGER" = "pnpm" ]; then pnpm run build; fi
            if [ "$MANAGER" = "yarn" ]; then yarn run build; fi
            if [ "$MANAGER" = "npm" ]; then npm run build; fi
          else
            echo "No build script; skipping."
          fi

      - name: Test (if present)
        if: steps.pm.outputs.has_node == 'true'
        shell: bash
        working-directory: ${{ matrix.path }}
        run: |
          set -euo pipefail

          if node -e "const p=require('./package.json'); process.exit(p.scripts && p.scripts.test ? 0 : 1)"; then
            MANAGER="${{ steps.pm.outputs.manager }}"
            echo "Running tests with: $MANAGER"
            if [ "$MANAGER" = "pnpm" ]; then pnpm test; fi
            if [ "$MANAGER" = "yarn" ]; then yarn test; fi
            if [ "$MANAGER" = "npm" ]; then npm test; fi
          else
            echo "No test script; skipping."
          fi
